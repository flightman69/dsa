#include<iostream>
#include<vector>
int* leaders(int arr[], int n){
  std::vector<int> vec;
  int max = n-1;
  vec.push_back(arr[max]);
  for(int i = n-2; i >= 0; i--){
    if(arr[i] >= arr[max])
  }
}
